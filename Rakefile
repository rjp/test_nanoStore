# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project'
require 'bubble-wrap'
require 'rubygems'
require 'motion-cocoapods'
require 'nano-store'
require 'time'

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.name = 'test_nanoStore'

  app.pods do
      pod 'NanoStore', '>= 2.1.4'
  end
end
